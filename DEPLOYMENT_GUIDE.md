# دليل النشر على GitHub Pages - خطوة بخطوة

## المتطلبات الأساسية
- حساب على GitHub (مجاني)
- Git مثبت على جهازك

## الخطوات التفصيلية

### 1️⃣ إنشاء حساب GitHub (إذا لم يكن لديك)
1. اذهب إلى [github.com](https://github.com)
2. اضغط "Sign up"
3. أدخل بريدك الإلكتروني وأنشئ كلمة مرور
4. تحقق من بريدك الإلكتروني

### 2️⃣ إنشاء مستودع جديد (Repository)
1. بعد تسجيل الدخول، اضغط على زر "+" في الأعلى
2. اختر "New repository"
3. أدخل اسم المستودع: `beni-haroun-site`
4. اختر "Public" (عام)
5. لا تضف README أو .gitignore (لدينا بالفعل)
6. اضغط "Create repository"

### 3️⃣ رفع الملفات إلى GitHub

#### الطريقة الأولى: باستخدام Git (موصى بها)
افتح PowerShell أو Command Prompt في مجلد المشروع وقم بتنفيذ:

```bash
# تهيئة Git في المجلد
git init

# إضافة جميع الملفات
git add .

# إنشاء أول commit
git commit -m "Initial commit - Barrage Beni Haroun Site"

# ربط المستودع المحلي بـ GitHub (استبدل USERNAME باسم المستخدم الخاص بك)
git remote add origin https://github.com/USERNAME/beni-haroun-site.git

# رفع الملفات
git branch -M main
git push -u origin main
```

#### الطريقة الثانية: رفع يدوي عبر الموقع
1. في صفحة المستودع على GitHub، اضغط "uploading an existing file"
2. اسحب جميع الملفات والمجلدات إلى الصفحة
3. اضغط "Commit changes"

### 4️⃣ تفعيل GitHub Pages
1. في صفحة المستودع، اضغط على "Settings"
2. من القائمة الجانبية، اضغط على "Pages"
3. في قسم "Source":
   - اختر Branch: `main`
   - اختر Folder: `/ (root)`
4. اضغط "Save"
5. انتظر دقيقة أو دقيقتين

### 5️⃣ الوصول إلى موقعك
بعد التفعيل، سيظهر رابط الموقع في أعلى صفحة Settings > Pages:
```
https://USERNAME.github.io/beni-haroun-site/
```

استبدل `USERNAME` باسم المستخدم الخاص بك على GitHub.

## 🔄 تحديث الموقع لاحقاً

عندما تريد تحديث الموقع:

```bash
# إضافة التغييرات
git add .

# إنشاء commit جديد
git commit -m "وصف التغييرات"

# رفع التحديثات
git push
```

سيتم تحديث الموقع تلقائياً في غضون دقائق.

## ⚠️ ملاحظات مهمة

1. **الوقت**: قد يستغرق ظهور الموقع من 2-10 دقائق بعد التفعيل
2. **HTTPS**: GitHub Pages يوفر HTTPS مجاناً تلقائياً
3. **النطاق المخصص**: يمكنك ربط نطاق خاص بك لاحقاً
4. **التخزين**: localStorage يعمل بشكل طبيعي
5. **الأمان**: جميع البيانات محفوظة في متصفح المستخدم فقط

## 🆘 حل المشاكل الشائعة

### الموقع لا يظهر
- تأكد من أن اسم الملف الرئيسي هو `index.html`
- تحقق من أن Branch المختار هو `main`
- انتظر 5-10 دقائق

### صفحة 404
- تأكد من أن جميع الملفات تم رفعها بشكل صحيح
- تحقق من أن مسارات الملفات صحيحة (حساسة لحالة الأحرف)

### الصور لا تظهر
- تأكد من أن مجلد `assets` تم رفعه
- تحقق من مسارات الصور في HTML/CSS

## 📞 الدعم
إذا واجهت أي مشاكل، يمكنك:
- مراجعة [توثيق GitHub Pages](https://docs.github.com/en/pages)
- البحث في [GitHub Community](https://github.community)

---
✅ بعد اتباع هذه الخطوات، سيكون موقعك متاحاً على الإنترنت مجاناً ومدى الحياة!
